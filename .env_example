# Copy to .env only if it does not already exist. Never commit real API keys.
# Set either key, or both for automatic fallback.
GEMINI_API_KEY=replace_with_your_gemini_api_key
OPENAI_API_KEY=replace_with_your_openai_api_key
